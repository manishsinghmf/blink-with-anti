import { a as BlinkContext, b as BlinkPreview } from '../useBlink-h5hSd-Ys.cjs';
export { B as Blink, c as BlinkProvider, f as fetchBlink, u as useBlink } from '../useBlink-h5hSd-Ys.cjs';
export { H as ActionError, h as ActionGetRequest, j as ActionGetResponse, m as ActionParameter, o as ActionParameterSelectable, n as ActionParameterType, A as ActionPostRequest, B as ActionPostResponse, s as ActionResponse, g as ActionRuleObject, i as ActionType, f as ActionsJson, C as CompletedAction, D as DialectExperimentalFeatures, E as ExternalLinkResponse, G as GeneralParameterType, I as InlineLink, w as InlineNextActionLink, r as LifecycleData, l as LinkedAction, L as LinkedActionType, F as MessageNextActionPostRequest, M as MinMax, x as NextAction, N as NextActionLink, y as NextActionPostRequest, q as OnActionError, O as OnActionExecuting, p as OnActionSuccess, P as PostActionType, v as PostNextActionLink, u as PostResponse, c as SOLANA_ACTIONS_PROTOCOL, d as SOLANA_PAY_PROTOCOL, S as SelectableParameterType, a as SignMessageData, z as SignMessageResponse, e as SupportedProtocols, t as TransactionResponse, k as TypedAction, T as TypedActionParameter } from '../supportability-u6EL3BkF.cjs';

interface DataRow {
    key: string;
    title: string;
    value: string;
    icon?: string;
    url?: string;
}
interface BlinkDataTable {
    rows: DataRow[];
    context: BlinkContext;
    links: {
        blink: string;
        preview: string;
    };
}
/**
 * This is a new version of the useMetadata hook, which uses Dialect's API to fetch updated models
 *
 * NOTE: not intended to be used with local urls (e.g. `localhost`)
 */
declare const useBlinkDataTable: ({ blinkApiUrl, wallet, }: {
    blinkApiUrl?: string | null;
    wallet?: string;
}) => {
    data: BlinkDataTable | null;
    isLoading: boolean;
    refresh: (options?: {
        abortController?: AbortController;
    }) => Promise<void>;
};
declare function fetchBlinkDataTable(blinkApiUrl: string, wallet?: string, options?: {
    abortController?: AbortController;
}): Promise<BlinkDataTable>;

interface BlinkList {
    id: string;
    name: string;
    entries: BlinkListEntry[];
}
type BlinkListEntry = BlinkPreview & {
    id: string;
    links: {
        blink: string;
        dataTable?: string;
    };
};
/**
 * Slightly modified version of the useBlinkList hook, which used Dialect API to fetch the blink list.
 *
 * NOTE: not intended to be used with local urls (e.g. `localhost`)
 */
declare const useBlinkList: ({ id, wallet, }: {
    id?: string | null;
    wallet?: string | null;
}) => {
    loading: boolean;
    refresh: (options?: {
        abortController?: AbortController;
    }) => Promise<void>;
    data: BlinkList | null;
};
declare function fetchBlinkList(id: string, wallet?: string | null, options?: {
    abortController?: AbortController;
}): Promise<BlinkList>;

type BlinkPreviewResponse = BlinkPreview & {
    links: {
        blink: string;
        dataTable?: string;
    };
};
/**
 * Hook to fetch blink preview - a lightweight version of the blink and not actionable, only for visual representation.
 *
 * NOTE: not intended to be used with local urls (e.g. `localhost`)
 */
declare const useBlinkPreview: ({ blinkApiUrl, }: {
    blinkApiUrl?: string | null;
}) => {
    preview: BlinkPreview | null;
    isLoading: boolean;
    refresh: (options?: {
        abortController?: AbortController;
    }) => Promise<void>;
};
declare function fetchBlinkPreview(blinkApiUrl: string, options?: {
    abortController?: AbortController;
}): Promise<BlinkPreviewResponse>;

export { BlinkContext, type BlinkDataTable, type BlinkList, type BlinkListEntry, BlinkPreview, type BlinkPreviewResponse, type DataRow, fetchBlinkDataTable, fetchBlinkList, fetchBlinkPreview, useBlinkDataTable, useBlinkList, useBlinkPreview };
